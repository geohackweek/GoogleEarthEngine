#------------------------------------------------------------
# Values for this lesson.
#------------------------------------------------------------
encoding: utf-8

# What kind of thing is this ("workshop" or "lesson")?
kind: "lesson"

# Overall title for pages.
title: "Google Earth Engine"

# Repository URL (without trailing slash).
repo: https://github.com/geohackweek/GoogleEarthEngine

# Root URL below username.github.io domain, with leading slash but no trailing slash.
# For example, for 'https://geohackweek.github.io/lesson-example', use '/lesson-example'.
root: "/GoogleEarthEngine"

# Contact email address.
email: arendta@uw.edu

#------------------------------------------------------------
# Generic settings (should not need to change).
#------------------------------------------------------------

# Is this production or development? (Overridden in _config_dev.yml.)
is_production: true

# Sites.
ghw_site: "https://geohackweek.github.io"
ghw_github: "https://github.com/geohackweek"
swc_site: "https://software-carpentry.org"
template_repo: "https://github.com/swcarpentry/styles"
example_repo: "https://github.com/swcarpentry/lesson-example"
example_site: "https://swcarpentry.github.com/lesson-example"

# Start time in minutes (540 is 09:00 am)
start_time: 540

# Specify that things in the episodes collection should be output.
collections:
  episodes:
    output: true
    permalink: /:path/
  extras:
    output: true

# Set the default layout for things in the episodes collection.
defaults:
  - scope:
      path: ""
      type: episodes
    values:
      layout: episode

# Files and directories that are not to be copied.
exclude:
  - Makefile
  - bin

# Turn off built-in syntax highlighting.
highlighter: rouge
